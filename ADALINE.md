Il modello **ADALINE** ( o **ADAptive LINear Element**) si basa sulla codifica per lo stato $false$ = -1 e per lo stato $true$ = 1.
Perciò, un input falso comporta, nel caso di output errato, ad un adattamento dei pesi.